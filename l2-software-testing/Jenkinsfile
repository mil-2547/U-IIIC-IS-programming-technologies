pipeline {
  agenty any
  stages {
    stage('Test') {
      steps {
        sh 'make build-unit'
      }
    }
    stage('Build') {
      steps {
        sh 'make build'
        archiveArtifacts artifacts: 'build/bin/app', fingerprint: true
      }
    }
  }
}
